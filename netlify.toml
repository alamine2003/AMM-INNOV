# ---------------------------------------------------------------------------
# Netlify — frontend AMM INNOV (SPA React/Vite). Backend sur Railway (railway.json).
# Les URL ci-dessous doivent correspondre au domaine public du service web Railway
# (Settings > Networking > Generate Domain, renommable), ou au domaine personnalisé api.<domaine>.
# Guide : docs/deploiement-netlify-railway.md
# ---------------------------------------------------------------------------
[build]
  base    = "frontend"
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "24"
  # Le client ajoute /api/v1 lui-même : ne pas mettre /api ici.
  VITE_API_BASE = "https://amm-innov-backend.up.railway.app"
  VITE_WS_URL   = "wss://amm-innov-backend.up.railway.app/ws/"

# Routage SPA : toutes les routes React renvoient index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options         = "DENY"
    X-Content-Type-Options  = "nosniff"
    Referrer-Policy         = "strict-origin-when-cross-origin"
    Permissions-Policy      = "camera=(), microphone=(), geolocation=()"

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/index.html"
  [headers.values]
    Cache-Control = "no-cache"
