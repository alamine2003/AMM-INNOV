# Base de l'API (vide = même origine, proxifié par Vite en dev vers http://localhost:8000)
VITE_API_BASE=
# URL WebSocket (vide = ws(s)://<host>/ws/)
VITE_WS_URL=
# 1 pour démarrer l'application avec les mocks MSW (sans backend) : npm run dev:mock
VITE_USE_MOCKS=
